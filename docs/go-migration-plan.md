# Go migration plan

Phased plan for completing the Python → Go rewrite on `feat/migrate-to-golang`.
Each phase is designed to be executed in **one short Claude Code session** with
minimal context, because the executor is on a usage-limited plan. Phases are
ordered by dependency; do them in order unless marked independent.

## How to execute a phase (instructions to the agent)

1. Read **only** the files listed under "Read" for that phase. Do not explore
   the rest of the repo — this plan plus CLAUDE.md is your context.
2. Port behavior faithfully from the Python source. The Python implementation
   is the spec; match its output byte-for-byte where it generates text
   (snippets, configs, headers), including marker lines and generated-file
   headers.
3. Port the listed Python tests to Go table-driven tests in the same package
   (`foo_test.go`). Use `t.TempDir()` for filesystem tests; never touch the
   real `$HOME`.
4. Verify: `go build ./...` then `go test ./...`. Both must pass.
5. Commit with the conventional message given for the phase. One commit per
   phase. Do not commit if tests fail.
6. Stop after the phase. Do not start the next phase in the same session.

### Standing rules (from CLAUDE.md, repeated here so the agent need not rediscover them)

- No third-party deps beyond `cobra` and `BurntSushi/toml`. `age` and `git`
  are invoked as external binaries via `os/exec` (matching Python).
- Errors: always return `*errs.DotsError` (or wrap) with a `Hint`. Copy hint
  text from the Python source.
- Idempotency: running any operation twice yields the same result.
- `--dry-run` produces zero side effects.
- Generated files get a "generated by dots" header; user files are backed up
  (`fileutil.Backup`) before overwrite.
- Reuse existing helpers in `internal/fileutil` (expand, sha256, backup,
  ensure-parent) instead of reimplementing them. Read `internal/fileutil/fileutil.go`
  first in every phase to see what already exists; add missing helpers there
  (e.g. marker-block insert/remove belongs in `internal/shell`, `run`-style
  exec wrapper can live in a new `internal/execx` or in `fileutil` — pick one
  in Phase 1 and stay consistent).

### Already done (do not redo)

- `internal/platform` — implemented.
- `internal/errs` — implemented.
- `internal/config` — `Load`, `FindRepoRoot`, full dots.toml parsing, tested.
- `internal/fileutil` — expand/sha256/backup helpers, tested.
- `internal/discovery` — implemented + tested.
- `internal/deploy` — symlink/copy/render + results, implemented + tested.
- `cmd/dots/commands.go` — all cobra command constructors exist; `apply`,
  `preview`, `status`, `diff`, `list` are wired to deploy/discovery. Subsystem
  commands (shell/git/ssh/tools/repos/env/presets/encrypt/decrypt/doctor/migrate)
  still call stub packages.

---

## Phase 0 — commit work in progress

**Goal:** get the working tree clean so each later phase is one atomic commit.

- Read: `git status` / `git diff` only.
- Action: review and commit the pending deploy/discovery/fileutil work
  (currently modified/untracked) as e.g.
  `feat(deploy): implement file deployment and discovery with tests`.
  Split into two commits (discovery, deploy) if the diff separates cleanly.
- Verify: `go test ./...` green, `git status` clean.

## Phase 1 — internal/shell

**Goal:** snippet generation + marker-delimited insertion. Foundation for
git/ssh/presets phases (they reuse the marker-block pattern and generated
headers).

- Read: `src/dots/shell.py`, `src/dots/utils.py`, `internal/shell/shell.go`,
  `internal/config/config.go` (Shell/Tool types only), `internal/fileutil/fileutil.go`,
  `tests/unit/test_snippets.py`, `adr/007-marker-delimited-insertion.md`.
- Implement in `internal/shell`: `GenerateEnvSnippet`, `GeneratePathSnippet`,
  `GenerateToolSnippet`, custom-snippet discovery (port
  `generate_custom_snippet`), `WriteSnippets`, `InsertSourceLine`
  (port `idempotent_insert`), `Clean` (port `remove_marker_block`),
  `Assembled`.
- Port tests: `tests/unit/test_snippets.py` → `internal/shell/shell_test.go`.
- Commit: `feat(shell): implement snippet generation and marker insertion`

## Phase 2 — internal/git + internal/ssh (one session, they're small twins)

**Goal:** managed gitconfig and ssh config generation.

- Read: `src/dots/git.py`, `src/dots/ssh.py`, `internal/git/git.go`,
  `internal/ssh/ssh.go`, relevant Config structs,
  `tests/unit/test_git_config.py`, `tests/unit/test_ssh_config.py`.
- Implement: `git.GenerateConfig/WriteManaged/ShowManaged/Uninit`,
  `ssh.GenerateConfig/WriteManaged/ShowManaged/Uninit` (incl.
  `snake_to_ssh_keyword` mapping, Include-line insertion in
  `~/.ssh/config` per `ssh_init`).
- Port both unit test files.
- Commit: `feat(git,ssh): implement managed gitconfig and ssh config generation`

## Phase 3 — internal/secrets

**Goal:** age encrypt/decrypt via `os/exec`.

- Read: `src/dots/secrets.py`, `internal/secrets/secrets.go`,
  `tests/integration/test_secrets.py`.
- Implement `Encrypt`, `Decrypt`, `DecryptToMemory` shelling out to `age`;
  copy the exact error hints (missing binary, missing identity, decrypt
  failure). Wire `newEncryptCmd`/`newDecryptCmd` in `cmd/dots/commands.go`
  (port `cmd_encrypt`/`cmd_decrypt` from `src/dots/commands.py:690-713`).
- Tests: unit-test the command construction and error paths with `age`
  absent; gate real round-trip tests behind `testing.Short()` /
  `exec.LookPath("age")` skip.
- Commit: `feat(secrets): implement age encrypt/decrypt`

## Phase 4 — internal/presets

**Goal:** preset generation (fzf, zprofile, profile) + eject.

- Read: `src/dots/presets.py`, `internal/presets/presets.go`,
  `adr/009-preset-eject-pattern.md`, `adr/010-profile-auto-activation.md`,
  `tests/unit/test_profiles.py` (profile parts).
- Implement `Generate`, `Eject`; output must match Python byte-for-byte.
- Commit: `feat(presets): implement preset generation and eject`

## Phase 5 — internal/tools

**Goal:** the biggest module — tool install via GitHub releases, plus other
install methods. Likely the longest phase; if a session runs low, stop after
the GitHub-release path + tests and finish remaining methods in a follow-up
session (split commits: `feat(tools): github release install`,
`feat(tools): remaining install methods`).

- Read: `src/dots/tools.py`, `internal/tools/tools.go`, Tool/ToolInstall
  config structs, `adr/006-github-releases-first.md`,
  `adr/013-arch-map-per-tool-override.md`, `tests/unit/test_tools.py` (the
  unit file, not the 450-line integration file — port integration tests only
  if budget allows).
- Implement: `Check` (installed detection), `Filter`, `Install` dispatch,
  GitHub latest-release lookup + asset selection (stdlib `net/http`,
  `encoding/json`), safe tar/zip extraction (port `_safe_tar_extractall` /
  `_safe_zip_extractall` path-escape checks — security-relevant, keep them),
  binary install into bin dir.
- Commit(s): see above.

## Phase 6 — internal/repos

**Goal:** repo clone/update/status via `git` subprocess.

- Read: `src/dots/repos.py`, `internal/repos/repos.go`,
  `tests/integration/test_repos.py`.
- Implement `Clone`, `Update`, `Status`, `Filter`. Tests can use local
  file:// remotes created in `t.TempDir()` (mirror the Python integration
  tests).
- Commit: `feat(repos): implement repo cloning and updating`

## Phase 7 — CLI wiring: subsystem dispatch + apply orchestration

**Goal:** make every cobra command call the now-real packages, and port the
`cmd_apply` orchestration order.

- Read: `src/dots/commands.py` (`cmd_apply`, `_apply_shell`, `_apply_git`,
  `_apply_repos`, `_apply_presets`, `_apply_login_shell`, `_apply_tools`,
  `dispatch_*`), `cmd/dots/commands.go`, `tests/integration/test_apply.py`.
- Wire: shell/git/ssh/env/tools/repos/presets subcommands; extend `apply` to
  run subsystems in the same order as Python. Honor `--dry-run` everywhere.
- Port the apply integration tests (`internal/` package test or a
  `cmd/dots` test driving the cobra root with a temp HOME).
- Commit: `feat(cli): wire subsystem commands and full apply orchestration`

## Phase 8 — doctor, migrate, init, edit, add

**Goal:** remaining commands.

- Read: `src/dots/commands.py` (`cmd_doctor`, `cmd_migrate`, `cmd_init`,
  `cmd_edit`, `cmd_add`), `cmd/dots/commands.go`.
- Implement and wire; doctor checks should cover every subsystem implemented
  above.
- Commit: `feat(cli): implement doctor, migrate, init, edit and add`

## Phase 9 — parity check + Python removal

**Goal:** prove parity, then drop Python.

- Run the Python integration suite mentally against the Go binary: for each
  file in `tests/integration/`, confirm a Go equivalent exists; port any
  gaps.
- Manual smoke: `just build`, then `bin/dots --help`, `dots apply --dry-run`
  against a fixture repo, `dots doctor`.
- Remove `src/dots/`, `tests/` (Python), `pyproject.toml`; update `justfile`
  and `install.sh` for Go-only; update `release-please-config.json` if it
  references Python packaging.
- Commit: `feat!: complete Go migration, remove Python implementation`
  (check ROADMAP.md first — if the Python removal should wait for a release,
  split into `feat: ...` now and removal later).

## Phase 10 — docs + ADRs

**Goal:** documentation reflects the Go implementation.

- Write `adr/014-go-rewrite.md`: motivation (single static binary, no Python
  runtime dependency), consequences, and note that it **supersedes**
  `adr/001-single-python-file.md`, `adr/011-package-split.md`, and
  `adr/012-minimum-python-version.md`. Add a "Superseded by ADR-014" status
  line to those three ADRs (do not delete them).
- Update `docs/architecture.md` (package layout = `cmd/` + `internal/`),
  `docs/contributing.md` (Go toolchain, just commands),
  `docs/migration.md`, `docs/troubleshooting.md` (error/hint examples),
  `docs/configuration.md` (only if behavior diverged — it shouldn't),
  `README.md` (install instructions), and CLAUDE.md (drop the Python
  reference sections, mark stubs as implemented, rewrite the "Adding a new
  managed subsystem" checklist in Go terms).
- Commit: `docs: update docs and ADRs for the Go rewrite`

---

## Post-migration fixes (review of 2026-06-11)

All 10 original phases are committed and `go build/vet/test` are green, but a
code review found bugs, parity gaps, and invariant violations. Fix them in the
phases below — same rules as above: read **only** the listed files, one commit
per phase, `go build ./... && go test ./...` green before committing, stop
after the phase.

### Phase 11 — correctness bugs (one session, six small fixes)

Read: `internal/shell/shell.go`, `internal/ssh/ssh.go`, `cmd/dots/main.go`,
`internal/tools/tools.go`, `cmd/dots/commands.go` (only `runApply`,
`printResults`, `applyPresets`), `internal/shell/shell_test.go`,
`internal/ssh/ssh_test.go`.

1. **Tool-snippet guard is wrong** — `internal/shell/shell.go:135-141`
   (`GenerateToolSnippet`): when `tool.Check` starts with `"which "`, the
   guard is set to `parts[0]` which is the literal string `which`, so
   `command -v which` always succeeds. Use `parts[1]` (the binary name).
   Add a test case: `Check: "which zoxide"` → snippet contains
   `command -v zoxide`.
2. **SSH config output is non-deterministic** — `internal/ssh/ssh.go:78`:
   `for k, v := range host.Options` iterates a map in random order, so
   `managed.gitconfig`'s sibling SSH fragment differs run-to-run (breaks
   invariant 3, idempotency). Collect keys, `sort.Strings`, then emit. Add a
   test generating the config twice for a host with ≥3 options and asserting
   byte equality.
3. **Non-DotsError errors are silent** — `cmd/dots/main.go:13-20`: the root
   command has `SilenceErrors: true` and main only prints when
   `errs.Unwrap(err)` matches a dots error type. A plain error (e.g. an
   `os.MkdirAll` failure bubbling up from `runInit`) exits 1 with **no output
   at all**. Add an `else` branch: `fmt.Fprintf(os.Stderr, "\n✗ %v\n", err)`.
   Also delete the dead `todo()` helper at the bottom of main.go.
4. **GitHub asset download ignores HTTP status** — `internal/tools/tools.go`
   `githubDownloadAsset`: a 404/403 response body (JSON error) is written to
   disk and later installed as the binary. After `httpClient.Do`, return a
   `errs.NewTool` error when `resp.StatusCode != http.StatusOK` (hint: include
   the status code and URL). Add a test using the existing `httpClient`/
   `githubAPIBase` test seams with an httptest server returning 404.
5. **Unknown archive types installed raw** — `internal/tools/tools.go`
   `installGitHub` switch: only `.tar.gz/.tgz/.zip` are extracted; a
   `.tar.xz`/`.tar.bz2`/`.tar.zst` asset falls through to `installBinaryFile`
   and the compressed archive itself is installed as the "binary". Before the
   default branch, detect these suffixes and return `errs.NewTool` with hint
   "Use the 'asset' field to select a .tar.gz or .zip asset." Test with a
   fake asset named `foo.tar.xz`.
6. **`dots apply` exits 0 on deploy errors** — `cmd/dots/commands.go`
   `runApply`/`printResults`: per-file errors are printed to stderr and
   counted but never affect the return value. Make `printResults` return the
   error count and have `runApply` return
   `errs.New(fmt.Sprintf("%d file(s) failed to deploy", n), "See errors above.")`
   when n > 0 (after running the remaining subsystems is NOT required —
   returning early after printResults is fine and simpler).

Commit: `fix: correct tool guard, ssh ordering, silent errors, download checks`

### Phase 12 — `dots shell clean` deletes the fzf preset + write hygiene

Read: `cmd/dots/commands.go` (`applyPresets`, `applyLoginShell`),
`internal/shell/shell.go` (`Clean`), `internal/fileutil/fileutil.go`,
`internal/shell/shell_test.go`, `cmd/dots/commands_test.go`.

1. **Clean/apply disagreement**: `applyPresets` writes `030-fzf.sh` into
   shell.d, but `shell.Clean`'s `expected` map only contains `010-env.sh`,
   `020-path.sh`, `050-*.sh`, `099-custom.sh` — so `dots shell clean` deletes
   the preset that `dots apply` just wrote. Fix: add `"030-fzf.sh"` to
   `expected` when `cfg.Presets.Fzf` is true (and keep the two in sync — add
   a comment in both places pointing at the other). Test: apply-style write
   then Clean, assert the file survives.
2. **User files overwritten without backup** (invariant 8) and rewritten when
   unchanged (invariant 3): `applyPresets` (tmux → `~/.tmux.conf`) and
   `applyLoginShell` (→ `~/.zprofile`, `~/.profile`) call `os.WriteFile`
   unconditionally. For each target: read existing content; if identical, skip
   (and print nothing or "unchanged"); if the existing file does **not** start
   with a dots generated header (`shell.GeneratedHeader` or the
   `# Generated by dots preset:` prefix), call `fileutil.Backup` before
   writing. The fzf snippet lives inside shell.d (dots-owned), no backup
   needed there.
3. `internal/tools/tools.go` defines a private `expandHome` duplicating
   `fileutil.Expand` — delete it and use `fileutil.Expand` (CLAUDE.md:
   never reimplement helpers).

Commit: `fix: stop shell clean removing presets; back up user files on overwrite`

### Phase 13 — files.d platform discovery + Platforms() threading

The biggest parity gap. ADR 004 specifies `files.d/{platform}/` scoping;
`dots init` creates `files.d/`, `dots migrate --platform` writes into it, and
docs/platforms.md describes it — but `discovery.Walk` never reads it (its own
doc comment admits "not yet implemented"). Separately, commit ff58445 added
`platform.Platforms()` (multi-tag, e.g. `["linux","wsl"]`) but only
`dots env check` uses it; deploy filtering, discovery, ssh host filtering, and
env-snippet generation still match against the single `platform.Detect()` tag,
so `only = ["wsl"]` entries never apply anywhere that matters — and
`dots env check` (multi-tag) can report ✓ for a var that
`shell.GenerateEnvSnippet` (single-tag) omits.

Read: `adr/004-files-d-platform-scoping.md`, `docs/platforms.md`,
`internal/discovery/discovery.go` + test, `internal/deploy/deploy.go`
(`Apply`/`Status` platform filter, `containsPlatform`, `currentPlatform`),
`internal/shell/shell.go` (`generateEnvSnippet` only),
`internal/ssh/ssh.go` (`GenerateConfig` only), `internal/platform/platform.go`,
call sites in `cmd/dots/commands.go` that pass `platform.Detect()`.

1. Change the platform parameter to `[]string` (the result of
   `platform.Platforms()`) in: `discovery.Walk`, the deploy `Options` (add
   `Platforms []string`; delete the unused `currentPlatform(repoRoot)`
   helper whose arg is already ignored), `ssh.GenerateConfig`/`WriteManaged`/
   `ShowManaged`, and `shell.generateEnvSnippet`. Matching rule everywhere:
   entry is active if `only` is empty or intersects the tag list.
2. In `discovery.Walk`, after walking `files/`, walk
   `files.d/<tag>/` for each tag in the platform list (the `walkDir` `only`
   parameter already exists for this — pass `[]string{tag}`). files.d entries
   override same-`Dst` entries from `files/` (later wins), and explicit
   `[[file]]` entries still override everything (existing `mergeEntries`).
   Follow ADR 004 for the precedence order if it differs — the ADR is the
   spec.
3. Port/extend tests: discovery test with `files.d/linux/` and `files.d/wsl/`
   fixtures; deploy filter test with `Only: ["wsl"]` and
   `Platforms: ["linux","wsl"]`; assert `env check` and
   `GenerateEnvSnippet` agree (same tag list in, same vars out).

Commit: `feat(discovery): implement files.d platform scoping with multi-tag matching`

### Phase 14 — deploy .age secrets (and decide on templates)

`deploy.Apply` (internal/deploy/deploy.go:37-39) silently skips any entry
with `Template` or `Secret` set, yet discovery flags `.age`/`.j2` files,
`secrets.DecryptToMemory` exists precisely "to render decrypted files
in-memory" and is never called, and CLAUDE.md claims deploy is
"symlink/copy/render — implemented".

Read: `internal/deploy/deploy.go`, `internal/secrets/secrets.go`,
`internal/discovery/discovery.go` (flagging only), `adr/008-age-for-secrets.md`,
`internal/deploy/deploy_test.go`, `internal/secrets/secrets_test.go`.

1. Secrets: in `deploy.Apply`, when `entry.Secret`, call
   `secrets.DecryptToMemory(src, cfg)` and write the plaintext to dst with
   mode `0600` (always copy mode — never symlink a decrypted secret into the
   repo). Unchanged-detection: compare plaintext hash against dst. On
   missing age/identity return the error in `Result.Err` (the hints are
   already in the secrets package). Dry-run: report "decrypt" action, no
   side effects, and do not require age to be installed. This needs cfg
   plumbed into `deploy.Options` (add `Config config.Config` or just
   `Secrets config.SecretsConfig` — prefer the smaller one).
   Gate real-age tests behind `exec.LookPath("age")` skip like
   secrets_test.go does.
2. Templates: `.j2` rendering was never specced in Go (no ADR). Do **not**
   implement Jinja2. Keep skipping `Template` entries but make it visible:
   report action "skipped (template — not supported)" once, and update
   CLAUDE.md + docs/architecture.md to say render means "secret decryption";
   templates are unsupported in the Go version. If ROADMAP.md mentions
   templates, leave it — this is a docs correction, not a feature decision.

Commit: `feat(deploy): decrypt .age secrets on apply`

### Phase 15 — idempotency + small cleanups

Read: `cmd/dots/commands.go` (`runAdd`, `runDoctor`, `newCompletionCmd`),
`cmd/dots/commands_test.go`.

1. **`dots add` is not idempotent** (invariant 3): running it twice appends a
   duplicate `[[file]]` block to dots.toml. Before appending, check whether
   `globals.cfg.Files` already contains an entry with the same src or dst
   (or the repo dest file already exists) and print "already managed" +
   return nil instead.
2. **doctor dead code**: `filepath.Glob(".../**/*.age")` does not recurse in
   Go — delete the Glob call and keep only the WalkDir fallback, and make
   WalkDir skip `.git` (`if d.IsDir() && d.Name() == ".git" { return
   filepath.SkipDir }`).
3. **doctor exit code**: warnings currently produce exit 1, same as errors.
   Decide: errors → 1, warnings only → 0 (doctor is advisory; matches
   common CLI convention). Update `runDoctor` and any test.
4. `newCompletionCmd`: drop the no-op `DisableFlagParsing: false` line and
   the unreachable-in-practice `default:` branch can stay (harmless) — just
   use `errs.New` instead of constructing `&errs.DotsError{}` directly, for
   consistency.

Commit: `fix(cli): make add idempotent, clean up doctor checks and exit codes`

---

## Second review (2026-06-11, after phases 11–15)

A second pass compared the Go code against the last Python implementation
(`git show 'ed9aa5d^:src/dots/<file>'` — ed9aa5d is the commit that removed
`src/dots/`). It found one large missing feature and several parity gaps the
first review missed. Same rules: read only the listed files, one commit per
phase, `go build ./... && go test ./...` green before committing, stop after
the phase. Use `git show 'ed9aa5d^:src/dots/commands.py'` etc. to read the
Python spec where referenced.

### Phase 16 — deploy user snippets from shell/ (missing feature)

Python's `_apply_shell` (commands.py:365-450) copied every file from the
repo's `shell/` directory into shell.d, warning when a numeric prefix fell
outside the ranges 030-049, 080-089, 090+. The Go version never reads
`shell/` at all — yet `dots init` creates it, and docs/architecture.md
lists "Deploy user snippets from shell/" as apply step 3. Worse,
`shell.Clean`'s `expected` map doesn't know these files, so `dots shell
clean` deletes any user snippet that was deployed by the Python version.

Read: `internal/shell/shell.go`, `internal/shell/shell_test.go`,
`cmd/dots/commands.go` (`applyShell` only), and the Python
`_apply_shell` via git show.

1. In `WriteSnippets` (or a sibling `DeployUserSnippets` called from it):
   if `<repoRoot>/shell/` exists, copy each regular file into shell.d
   (skip names failing `fileutil.ShouldSkip`). Print the prefix-range
   warning exactly as Python does. `.j2` files in shell/: do **not**
   render (no Jinja2 in Go, same decision as Phase 14) — print
   "skipped (template — not supported)" instead of installing them.
2. Add the deployed names to `Clean`'s `expected` map (derive both from
   one helper listing shell/ contents so they cannot drift).
3. Honor dry-run. Tests: shell/ fixture with an in-range file, an
   out-of-range prefix (warning), and a `.j2` (skipped); plus a Clean
   test asserting deployed user snippets survive.

Commit: `feat(shell): deploy user snippets from the shell/ directory`

### Phase 17 — tool snippet parity

Python (commands.py:421-442) skipped tools with no `shell.env` and no
`shell.init`, and when `shell.init` contained `{shell}` it wrote per-shell
variants `050-{name}.zsh` + `050-{name}.bash` (PR #9); otherwise a single
`050-{name}.sh` generated with shell_name="bash". The Go `WriteSnippets`
writes one `050-{name}.sh` for **every** tool (empty guard-only snippets
for tools with no shell config) and hardcodes shell_name="zsh" — so bash
users source zsh-flavored init lines, regressing PR #9.

Read: `internal/shell/shell.go` (`WriteSnippets`, `GenerateToolSnippet`,
`Clean`), `internal/shell/shell_test.go`, Python `_apply_shell` via git
show.

1. Skip tools where `len(Shell.Env) == 0 && Shell.Init == ""`.
2. If `Shell.Init` contains `{shell}`: write `050-{name}.zsh` (zsh) and
   `050-{name}.bash` (bash). Else: write `050-{name}.sh` with
   shell_name="bash" (matching Python's default).
3. Mirror the same naming logic in `Clean`'s `expected` map (shared
   helper, same anti-drift comment as fzf).
4. Tests for all three shapes; assert Clean keeps the new names and
   removes a stale `050-{name}.sh` left over after a tool gains
   `{shell}` in its init.

Note (decide, then document — no code change): Python named the custom
snippet `000-custom.sh` (sourced before env/path); Go writes
`099-custom.sh` (sourced last) and docs/architecture.md still says 000.
Keeping 099 is reasonable (custom overrides generated), but then update
docs/architecture.md and add a line to docs/migration.md that
Python-era `000-custom.sh` is removed by `dots shell clean`.

Commit: `fix(shell): match Python tool-snippet generation and naming`

### Phase 18 — tools ignore their platform scoping (Tool.Only)

`Tool.Only` is parsed into the config but never read: Python filtered
`[t for t in config.tools if matches_platform(t.only, plat)]` in
`_apply_tools` **and** in the `tools check/install/list` dispatch
(commands.py:715-758). In Go, a tool with `only = ["darwin"]` is checked,
installed, listed — and gets a 050 shell snippet — on every platform.

Read: `internal/tools/tools.go` (`Filter`, `Check`), `cmd/dots/commands.go`
(`applyTools`, `newToolsCmd`), `internal/shell/shell.go` (`WriteSnippets`,
`Clean`), `internal/tools/tools_test.go`.

1. Add platform filtering to `tools.Filter` (new `platforms []string`
   parameter, matching rule: `Only` empty or intersects — same as
   Phase 13) and apply it in `applyTools` and all three `tools`
   subcommands, passing `platform.Platforms()`.
2. Apply the same filter in `shell.WriteSnippets`/`Clean` so
   platform-excluded tools get no snippet (and theirs are cleaned).
3. `tools.Check`'s `plat, arch` parameters are currently unused — either
   use them for the filtering or drop them; don't leave dead parameters.
4. Tests: tool with `Only: ["darwin"]` on `Platforms: ["linux","wsl"]`
   is filtered everywhere.

Commit: `fix(tools): honor tool-level only platform scoping`

### Phase 19 — FileEntry.Mode is ignored + copy never replaces a symlink

Two deploy parity gaps against Python deploy.py:

Read: `internal/deploy/deploy.go`, `internal/deploy/deploy_test.go`,
Python deploy.py via git show (`_apply_mode`, the copy/secret branches).

1. **`mode` is parsed but never applied.** Python ran
   `_apply_mode(dst, entry.mode)` after every copy/secret/unchanged-copy
   path, and `_write_secret` defaulted to "600". In Go, add: after a
   successful copy (and on the "unchanged" copy path), if
   `entry.Mode != ""` parse it as octal (`strconv.ParseUint(mode, 8, 32)`)
   and `os.Chmod(dst, ...)`; for secrets use `entry.Mode` when set, else
   keep 0600. Invalid mode string → `Result.Err` with a hint showing the
   accepted format. Symlink entries: mode is meaningless — ignore it
   (Python did not chmod symlinks either).
2. **Copy mode never converts an existing symlink.** `applyCopy` hashes
   `dst` through the symlink, sees equal content, and returns "unchanged",
   so `--copy` (or `default_mode = "copy"`, or `link = false`) leaves the
   symlink in place forever. Python treated "dst is a symlink" as
   always-replace: backup, unlink, copy. Match that: in `applyCopy`, check
   `os.Lstat` mode for `ModeSymlink` before the hash comparison and go
   straight to backup+replace. Optionally make `Status` report such
   entries as "diff" rather than "linked" ✓ (it currently calls any
   correct-target symlink "linked" even when the entry wants a copy).
3. Tests: `mode = "600"` on a copy entry; secret honoring custom mode;
   copy entry over an existing correct symlink → "copied" and dst is a
   regular file.

Commit: `fix(deploy): apply file modes and convert symlinks in copy mode`

### Phase 20 — cosmetics and stale docs

Read: `cmd/dots/commands.go` (`printResults`, `applyPresets`),
`internal/shell/shell.go` (`Clean`), `internal/tools/tools.go`
(`safeTarExtractAll`), `docs/architecture.md`.

1. `printResults` summary line never counts secrets: include
   "N decrypted" (covering both the dry-run "decrypt" and real
   "decrypted" actions) when nonzero.
2. fzf condition mismatch: `applyPresets` writes `030-fzf.sh` only when
   `cfg.Presets.Fzf && cfg.Shell.Managed`, but `shell.Clean` expects it
   whenever `cfg.Presets.Fzf`. Align Clean to the same compound condition.
3. `tar.TypeRegA` is deprecated (and normalized to `TypeReg` by the tar
   reader anyway) — drop it from the switch in `safeTarExtractAll`.
4. docs/architecture.md is stale: "tools/ … stub pending implementation"
   (line ~24), and the apply-steps list still reflects Python (000-custom
   naming — fix per the Phase 17 decision). Update to match the Go code.

Commit: `chore: fix result counts, clean conditions, and stale docs`

### Reviewed and intentionally not changed

- Dry-run deploy reports "link"/"copy" without checking current state, so
  `dots preview` doesn't say "unchanged" — Python printed
  `SYMLINK/COPY → dst` unconditionally too. Parity; leave.
- `InsertBlock` writes through a symlinked `~/.zshrc` into the repo's
  `files/.zshrc` (which then feeds 099-custom.sh). Python's
  `idempotent_insert` had the identical behavior. Pre-existing design
  quirk, not a migration bug; leave.
- `Tool.Profile` is parsed but unused — it was equally unused in the
  Python version. Leave (or delete the field in a later cleanup).

### Known limitations (documented, not scheduled)

- tar extraction validates symlink entries but never creates them (only
  dirs and regular files are extracted). Fine for single-binary releases;
  revisit only if a real tool needs in-archive symlinks.
- `git.GenerateConfig` output depends on `exec.LookPath("delta")`, so
  `dots git show` differs between machines. Intentional (matches "only enable
  delta when present") — do not "fix".

## Session budget notes (for the human)

- Phases 1–4, 6, 8 are small (one module each, ~100–300 lines of Go +
  tests) — comfortably one Pro session each, likely on Sonnet.
- Phase 5 (tools) and Phase 7 (apply orchestration) are the big ones —
  consider Opus, fresh session, and use the built-in split point in Phase 5.
- Always `/clear` between phases; the plan file is the only carry-over
  context the agent needs.
- If a phase ends with failing tests and the session dies, the next session's
  first instruction should be: "Finish phase N of docs/go-migration-plan.md;
  tests currently failing, fix then commit."
